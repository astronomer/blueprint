dag_id: getting_started_pipeline
schedule: "@daily"
description: One extract step feeding one load step

steps:
  extract_customers:
    blueprint: extract
    source: customers
    batch_size: 500

  load_customers:
    blueprint: load
    depends_on: [extract_customers]
    target: warehouse.customers
